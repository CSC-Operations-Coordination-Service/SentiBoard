# Copy this file to `.env.local` (git-ignored) and adjust as needed.
# Read on the SERVER only (never shipped to the browser) — see lib/data.ts.

# Base URL of the SentiBoard Flask backend.
BACKEND_URL=http://127.0.0.1:5005
